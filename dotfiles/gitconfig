[core]
    editor = vim

[init]
    defaultBranch = main

[pull]
    rebase = false

[alias]
    st = status
    co = checkout
    br = branch
    lg = log --oneline --graph --decorate --all

# 名前とメールアドレスは、リポジトリで共有しないローカルファイルに保存する。
[include]
    path = ~/.gitconfig.local
